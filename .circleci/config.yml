version: 2.1

jobs:
  build:
    docker:
      - image: circleci/openjdk:latest
    steps:
      - checkout
      - run: git submodule sync
      - run: git submodule update --init
      - run: ./gradlew test
